Logs are incorrectlly grouped with "None". They still appear as sorted by their lines not by their flat time.
Fix indexing of logs with format parameters. Right now they are ignoring the value of the parameter because we are searching against the format string.